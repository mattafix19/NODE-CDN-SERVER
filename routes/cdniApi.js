var express = require('express');
var router = express.Router();
var connectionService = require('./../services/ConnectionService');

router.get('/createOffer', function (req, res, next) {
    
});

module.exports = router;